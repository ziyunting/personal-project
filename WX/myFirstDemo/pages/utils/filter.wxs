  var filter = function (text) {
  if (!text) {
    return
  } else {
    var reg = getRegExp("\\n", 'g');
    return text.replace(reg, '\n&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;')
  }
}
module.exports = filter
